"use strict";
// function getRandomElement<T>( arr: T[] ):T {
//   const rndIndex = Math.floor((Math.random() * arr.length));
//   return arr[rndIndex];
// }
// Need to add , in the <T,> declaration
// const getRandomElement = <T,> ( arr: T[] ):T => {
//   const rndIndex = Math.floor((Math.random() * arr.length));
//   return arr[rndIndex];
// }
